#!/usr/bin/env bash
set -euo pipefail

echo "[pre-commit] Running quality gate..."
uv run --no-sync --locked ruff format . --check
uv run --no-sync --locked ruff check .
uv run --no-sync --locked mypy
uv run --no-sync --locked pytest
